
spring:
  data:
    mongodb:
      uri: ${sm://mongo-uri}
      authentication-database: ppm

logging:
  level:
    se:
      acrend:
        ppm: debug

    org:
      springframework: info
      mongodb: warn
    reactor: warn
